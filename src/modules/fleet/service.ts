// src/modules/fleet/service.ts — M2 owns this file
// Implemented in H1–2. Stub only.

export const fleetService = {
  // TODO: listVehicles(filters, pagination) → Vehicle[]
  // TODO: createVehicle(data) → Vehicle + auditLog
  // TODO: updateVehicle(id, data) → Vehicle + auditLog (blocks ON_TRIP manual set)
  // TODO: retireVehicle(id) → Vehicle (blocks if ON_TRIP or OPEN maintenance)
  // TODO: openMaintenance(vehicleId, data) → MaintenanceLog + auto IN_SHOP + auditLog
  // TODO: closeMaintenance(id) → MaintenanceLog + auto AVAILABLE (unless RETIRED) + auditLog
};
