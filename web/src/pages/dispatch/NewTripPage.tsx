// NewTripPage.tsx — M3 implements in H3.5-5
// Trip form: cargo + distance → recommendation panel (top 3) → one-click apply → dispatch
// 409 race shows "Vehicle was just dispatched by another user" with refreshed pool
export function NewTripPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-6">New Trip</h1>
      {/* TODO M3: cargo + distance inputs */}
      {/* TODO M3: recommendation panel — top 3 scored pairs with breakdown chips */}
      {/* TODO M3: one-click apply → pre-fill vehicle/driver, then dispatch */}
      <p className="text-sm text-muted-foreground">Implemented in H3.5-5 (M3)</p>
    </div>
  );
}
