// UsersPage.tsx — M1 implements in H1-2
// ADMIN only: user list, role assignment, pending approvals
export function UsersPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-6">Users</h1>
      {/* TODO M1: user list, role assignment form, ACTIVE/INACTIVE toggle */}
      <p className="text-sm text-muted-foreground">Implemented in H1-2 (M1) — ADMIN only</p>
    </div>
  );
}
