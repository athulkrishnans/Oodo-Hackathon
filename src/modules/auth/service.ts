// src/modules/auth/service.ts — M1 owns this file
// Implemented in H1–2. Stub only.

export const authService = {
  // TODO: login(email, password) → { accessToken, user }
  // TODO: signup(email, password, name) → User (role = null, status = ACTIVE)
  // TODO: assignRole(adminId, targetUserId, role) → User + auditLog + notification
};
